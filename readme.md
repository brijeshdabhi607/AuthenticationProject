create a virtual-environment of python using > python -m venv venv
install django using pip command > pip install django
clone this authentication project 
run the server using python manage.py runserver
